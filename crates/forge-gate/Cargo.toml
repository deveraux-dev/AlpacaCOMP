[package]
name = "forge-gate"
version = "0.1.0"
edition = "2021"

[lib]
name = "forge_gate"

[dependencies]
libm = { version = "0.2", default-features = false }
sha2 = { version = "0.10", default-features = false }
