# Trading Day Report — Wednesday 2026-09-02 (first live day)

**Outcome: zero orders placed. Three honest refusals, one falsified hypothesis, one measured recalibration — shipped and armed for Thursday.**

## Timeline (all times MDT)

| Time | Event |
|---|---|
| 7:36 | Open report (DRY): spot $762.21, purity 49 pmy DIFFUSE, condor unbuildable — later traced to chain truncation (`limit=300` returns a `next_page_token`; full chain is 442 contracts). |
| 9:47 | Dispatch (DRY, full paginated chain): condor 798C/815C/724P/704P, $312 credit, oracle verdict ScheduledMaintenance (bull `++00000000000` / bear `-000000000000`), five of six gates green — **REFUSED: ChaoticBook** at 49 pmy. |
| 11:33 | Single retry per preauth: condor improved to 796C/815C/723P/703P, $334 credit, StructuralEquilibrium (bull `+000000000000` / bear `-000000000000`) — **REFUSED: ChaoticBook** at 46 pmy. Final for the day. |

## The finding

Purity read 46–49 pmy across four distinct books (closed, open, mid-morning,
midday). The input mass — per-contract quote sizes — is near-uniform by
construction on an options chain: market makers post similar sizes at every
strike. Replaying the saved chain proved the signal does not exist at any
scale:

| Input mass | Chain-wide | ±$5 window | ±$40 window |
|---|---|---|---|
| Quote sizes (`bs+as`) | 47 pmy | 82 pmy | 52 pmy |
| Daily volume (`dailyBar.v`) | **625 pmy** | — | 1165 pmy |

Strike-windowing was falsified empirically before writing any code. Volume
mass carries real concentration signal — and inverts the polarity: panic
*concentrates* chain volume, calm spreads it.

## The fix (operator-authorized, tested, shipped)

- Purity mass switched to `dailyBar.v` (ratio-preserving u64→u16 scaling).
- Gate rebanded two-sided: refuse < 400 pmy (volume-dead/stale chain) and
  > 7500 pmy (panic concentration). forge-gate constants untouched.
- 28/28 daemon tests green, including new pins for both refusal ends.
- Replay of the 11:33 chain under the new band: **ALL SIX GATES PASSED (DRY)**
  — the same chain that was refused live now ships, and the band still has
  teeth on both ends.

## Why zero orders is the system working

Every refusal spent zero syscalls, carried a full audit trail (both S13
thesis tokens + arbiter verdict), and was correct given the gate's
calibration at the time. The agent declined to trade on a signal it could
not trust, unattended, three times — then the calibration was fixed from
measurements, not guesses. Thursday 9:47 MDT dispatch is armed under renewed
bounded pre-authorization: qty 1, credit floor $250, negative limit, all six
gates, zero manual overrides.
